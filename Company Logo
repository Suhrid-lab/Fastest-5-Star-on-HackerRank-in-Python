import math
import os
import random
import re
import sys
from collections import Counter
chars = Counter(input()).items()
# -c[1] means sorting descending for counts 
for char, n in sorted(chars, key = lambda c: (-c[1], c[0]))[:3]:
    print(char, n)
